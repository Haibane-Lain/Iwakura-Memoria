.venv/
node_modules/
# Legacy dev data folder — user data now lives in %LOCALAPPDATA%\IwakuraMemoria
# (see README "How data is stored"); the entry stays for old copies.
data/
.pytest_cache/
pytest-cache-files-*/
static/dist/
__pycache__/
*.pyc
LanguageTool 6.9/

# Distribution build outputs (scripts/build/build.bat).
scripts/build/_bundle/
scripts/build/_pyinstaller-work/
scripts/build/build-run.log
dist/
# Bundled JRE for the installer — drop it next to LanguageTool before building.
_jre/