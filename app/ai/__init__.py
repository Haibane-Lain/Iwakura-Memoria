"""Lain: the AI assistant.

- ``providers.py``  DeepSeek (OpenAI-compatible) chat client
- ``tools.py``      tool schemas + server-enforced folder access
- ``agent.py``      the agent loop, confirmation continuation, compression
- ``sessions.py``   persistent, auto-saved chat sessions
"""
